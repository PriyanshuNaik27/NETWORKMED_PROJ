# ══════════════════════════════════════════════════════════════════════
# IMPORTANT: This project works best on Python 3.10
# Python 3.11+ may have compatibility issues with DeepChem
#
# Check your Python version:
#   python --version
#
# If you have Python 3.11/3.12/3.13, create a 3.10 environment:
#   conda create -n toxicity python=3.10
#   conda activate toxicity
# OR download Python 3.10 from: https://www.python.org/downloads/
# ══════════════════════════════════════════════════════════════════════

# ── Core Chemistry ────────────────────────────────────────────────────
rdkit>=2023.3.1

# ── Machine Learning ──────────────────────────────────────────────────
scikit-learn>=1.3.0
numpy>=1.24.0
pandas>=2.0.0
joblib>=1.3.0

# ── PyTorch (CPU only — needed by DeepChem) ───────────────────────────
# Install separately BEFORE deepchem:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0

# ── DeepChem ──────────────────────────────────────────────────────────
# Only works reliably on Python 3.10
# If install fails, skip it and use manual tox21.csv instead
deepchem==2.6.0

# ── Web App ───────────────────────────────────────────────────────────
streamlit>=1.28.0

# ── Utilities ─────────────────────────────────────────────────────────
requests>=2.31.0


# ══════════════════════════════════════════════════════════════════════
# INSTALL ORDER (copy-paste these one by one):
# ══════════════════════════════════════════════════════════════════════
#
#   pip install --upgrade pip
#   pip install rdkit
#   pip install scikit-learn numpy pandas joblib requests
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install deepchem==2.6.0
#   pip install streamlit
#
# ══════════════════════════════════════════════════════════════════════
# TOX21 DATASET (MANDATORY for real results)
# ══════════════════════════════════════════════════════════════════════
# DeepChem downloads this automatically when you run run_pipeline.py.
# If that fails (e.g. Python 3.13), download manually:
#
#   URL: https://deepchemdata.s3-us-west-1.amazonaws.com/datasets/tox21.csv.gz
#   Steps:
#     1. Download the file
#     2. Extract it (you get tox21.csv)
#     3. Place it at:  drug_toxicity_project/data/tox21.csv
#
# Without this file, the model trains on only 20 sample compounds
# and toxicity scores will NOT be reliable.
# ══════════════════════════════════════════════════════════════════════
